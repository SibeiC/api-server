---
services:
  api-server:
    image: ghcr.io/sibeic/api-server:latest
    container_name: api-server
    ports:
      - "127.0.0.1:8080:8080"
      - "127.0.0.1:8957:8957"
    volumes:
      - /opt/api-server:/opt/api-server
    restart: unless-stopped
